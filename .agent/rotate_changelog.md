# Rotate Changelog

1. Archive the current `changelog.md` under `.agent/archive/`.
2. Add a short rotated history entry describing the archive point.
3. Reset `.agent/changelog.md` with the standard header/table shape.
4. Commit both the archive and reset changelog in one commit.
