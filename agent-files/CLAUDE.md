# CLAUDE Instructions for `agent-files`

This directory is the project bootstrap template. When working here, Claude must
read `AGENTS.md` and follow it continuously, then follow the more specific
`.agent/*` instructions as needed.

## Required startup checks

Before making any changes, read in this order:

- `AGENTS.md`
- any files AGENTS.md tells you to read

## Ongoing instruction source

- Always prioritize `AGENTS.md` as the baseline instruction document for this
  project and re-check it whenever context changes before continuing.
